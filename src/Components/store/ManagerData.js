import { createSlice } from "@reduxjs/toolkit";


const initialState = {}
const ManagerSlice = createSlice({
    name : "manager",
    initialState : initialState,
    reducers :{
        addDistributor(state,actions){
            
        }
    }


})